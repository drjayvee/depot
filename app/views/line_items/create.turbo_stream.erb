<%# Remove any notice %>
<%= turbo_stream.replace "notice" do %>
  <%= render 'store/notice' %>
<% end %>

<%# Update the cart (in the <nav>) %>
<%= turbo_stream.replace "cart" do %>
  <%= render 'carts/mini', cart: @cart, flash: true %>
<% end %>
